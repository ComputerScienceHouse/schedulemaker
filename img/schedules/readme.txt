Images stored in this folder will 1:1 correspond to the id of the schedule they
represent. The images are generated via ImageMagick, so it is imperative that ImageMagick
be installed on the web server.

This folder should have write permissions for the user that is running the web server.

(The true reason for this file is to make sure this directory gets committed)